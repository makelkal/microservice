version: '2'
services:
  eureka:
    image: eureka
    expose:
      - "8761"
  customer:
    image: customer
    expose:
      - "8080"
    links:
     - eureka
  catalog:
    image: catalog
    expose:
      - "8080"
    links:
     - eureka
  order:
    image: order
    expose:
      - "8080"
    links:
     - eureka
  zuul:
    image: zuul
    links:
     - eureka
    expose:
      - "8080"
#  turbine:
#    build: turbineserver
#    links:
#     - eureka
#    ports:
#      - "8989:8989"
