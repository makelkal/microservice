version: '2'
services:
  eureka:
    build: eureka_server
    expose:
      - "8761"
  customer:
    image: customer
    expose:
      - "8080" 
  catalog:
    image: catalog
    expose:
      - "8080" 
  order:
    image: order
    expose:
      - "8080" 
  zuul:
    image: zuul_server
    expose:
      - "8080" 
#  turbine:
#    build: turbineserver
#    links:
#     - eureka
#    ports:
#      - "8989:8989"
