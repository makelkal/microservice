version: '2'
services:
  eureka:
    image: eureka
    expose:
      - "8761"
  customer:
    image: customer
    expose:
      - "8080"
    links:
     - eureka
  catalog:
    image: catalog:$GO_REVISION
    expose:
      - "8080"
    links:
     - eureka
