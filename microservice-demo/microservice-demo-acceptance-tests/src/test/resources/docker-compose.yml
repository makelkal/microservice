version: '2'
services:
  eureka:
    build: eurekaserver
    expose:
      - "8761"
  customer:
    iamge: customer
    expose:
      - "8080" 
  catalog:
    image: catalog
    expose:
      - "8080" 
  order:
    image: order
    expose:
      - "8080" 
  zuul:
    image: zuulserver
    expose:
      - "8080" 
  turbine:
    build: turbineserver
    links:
     - eureka
    ports:
      - "8989:8989"
